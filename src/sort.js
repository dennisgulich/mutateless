/**
 * creates a new sorted list
 *
 * @category Array
 * @param {array} list
 * @returns {array} new sorted list
 * @example
 *
 * sort([3,1,2])
 * // => [1,2,3]
 */
export const sort = list => [...list].sort()
